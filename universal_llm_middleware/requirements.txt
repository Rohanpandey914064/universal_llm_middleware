# ─────────────────────────────────────────────────────────────────────────────
# universal_llm_middleware — requirements.txt
# Python 3.13 compatible pinned versions
# ─────────────────────────────────────────────────────────────────────────────

# ── Web Framework & Gateway ──────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0

# ── Data Validation & Configuration ─────────────────────────────────────────
pydantic>=2.7.1
pydantic-settings>=2.2.1
python-dotenv>=1.0.1

# ── LLM Client SDKs ─────────────────────────────────────────────────────────
openai>=1.30.1
anthropic>=0.26.1

# ── NLP / AI Inference ──────────────────────────────────────────────────────
# spacy 3.8.x supports Python 3.13 (3.7.x does NOT)
spacy>=3.8.2
scikit-learn>=1.5.0
# numpy 2.x supports Python 3.13
numpy>=2.0.0
onnxruntime>=1.18.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-multipart>=0.0.9
anyio>=4.3.0
starlette>=0.37.2

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.7
pytest-mock>=3.14.0
